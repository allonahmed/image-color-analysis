import React from 'react';

/**
 * 
 * @returns modal for image selecting 
 */

export const ImageSelect : React.FunctionComponent = () => {
  return (
    <div style={{background:'green', width: '100%', height: '100%', padding: '0', margin: '0'}}>
      <h1>Select Image</h1>
      <nav>upload URL Camera</nav>
      <div>
        image upload if image upload
      </div>
    </div>
  );
};

